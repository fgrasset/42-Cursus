# http {
#     server {
#         listen 443 ssl;
#         server_name fgrasset.42.fr;

#         index index.html;

#         ssl_certificate /etc/nginx/ssl/ssl-cert.crt;
#         ssl_certificate_key /etc/nginx/ssl/ssl-cert.key;

#         ssl_protocols TLSv1.3;
#         ssl_ciphers 'TLS_AES_128_GCM_SHA256:TLS_AES_256_GCM_SHA384:TLS_CHACHA20_POLY1305_SHA256:ECDHE-RSA-AES128-GCM-SHA256';

#         location / {
#             root /usr/share/nginx/html;
#         }
#     }
# }


user www-data;
worker_processes auto;
pid /run/nginx.pid;
include /etc/nginx/modules-enabled/*.conf;

events { }

http {
    # server {
    #     listen 80;
    #     server_name localhost;

    #     return 301 https://localhost;
    # }

    server {
        listen 443 ssl;
		listen [::]:443 ssl;
        server_name localhost;

        ssl_certificate /etc/nginx/ssl/ssl-crt.crt;
        ssl_certificate_key /etc/nginx/ssl/ssl-key.key;

        ssl_protocols TLSv1.2 TLSv1.3;

        root /var/www/html; # TODO modify
        index index.php index.html index.htm;

        # Additional SSL configurations if needed

        location / {
            try_files $uri $uri/ =404;
        }

        # Uncomment the following block if you want to handle PHP
        # location ~ \.php$ {
        #     include snippets/fastcgi-php.conf;
        #     fastcgi_pass unix:/var/run/php/php7.4-fpm.sock;
        #     fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        #     include fastcgi_params;
        # }
    }

    # ##
    # # Basic Settings
    # ##

    # sendfile on;
    # tcp_nopush on;
    # tcp_nodelay on;
    # keepalive_timeout 65;
    # types_hash_max_size 2048;
    # # server_tokens off;

    # # server_names_hash_bucket_size 64;
    # # server_name_in_redirect off;

    # include /etc/nginx/mime.types;
    # default_type application/octet-stream;

    # ##
    # # SSL Settings
    # ##

    # ssl_protocols TLSv1 TLSv1.1 TLSv1.2; # Dropping SSLv3, ref: POODLE
    # ssl_prefer_server_ciphers on;

    # ##
    # # Logging Settings
    # ##

    # access_log /var/log/nginx/access.log;
    # error_log /var/log/nginx/error.log;

    # ##
    # # Gzip Settings
    # ##

    # gzip on;

    # # gzip_vary on;
    # # gzip_proxied any;
    # # gzip_comp_level 6;
    # # gzip_buffers 16 8k;
    # # gzip_http_version 1.1;
    # # gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    # ##
    # # Virtual Host Configs
    # ##

    # include /etc/nginx/conf.d/*.conf;
    # include /etc/nginx/sites-enabled/*;
}

